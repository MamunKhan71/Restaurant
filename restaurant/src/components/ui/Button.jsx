import React from 'react'

const Button = ({ title }) => {
    return (
        <button className="btn px-6 bg-primary border-none rounded-none uppercase font-secondary text-[#0A1425] font-bold">{title}</button>
    )
}

export default Button